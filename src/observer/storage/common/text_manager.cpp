#include "text_manager.h"

// TextManager::TextManager(const char *filename) : filename_(filename)
// {
//   fd_ = open(filename, O_RDWR | O_CREAT | O_EXCL, S_IREAD | S_IWRITE);
//   read(fd_, &bitmap_size_, sizeof(int));
// //   read()
// }

// size_t TextManager::get_page_num() { bitmap_.next_unsetted_bit(); }